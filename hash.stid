#!/bin/bash
# input: CSV file with students' id and their marks with ',' delimiter.
# output: md5sumed students' id and their marks.

cat "$1" | sort -t, -nrk2 | awk -F "," '{
    cmd="echo -n "$1"|md5sum "
    cmd|getline cksum
    printf "%s %5.2f \n", cksum, $2
    }'